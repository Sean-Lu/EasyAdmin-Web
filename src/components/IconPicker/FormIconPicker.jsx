import React from "react";
import IconPicker from "./index";

const FormIconPicker = ({ value, onChange, placeholder }) => {
	return <IconPicker value={value} onChange={onChange} placeholder={placeholder} />;
};

export default FormIconPicker;
